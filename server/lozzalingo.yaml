# =============================================================================
# Fat Big Quiz - Lozzalingo Configuration
# =============================================================================
# This file configures which shared packages are enabled and how they're wired.
# Mirrors the Python framework's lozzalingo.yaml pattern.

site:
  name: "Fat Big Quiz"
  tagline: "Quizzes, Game Shows, Whacky Wagers"
  baseUrl: "${FRONTEND_URL}"

# Enable/disable features
features:
  # Core infrastructure
  config: true
  logging: true
  email: true
  subscribers: true
  settings: true
  ops: true
  storage: true
  auth: true

  # Experiences and bookings
  experiences: false  # FBQ uses EventProduct model (custom controller) to avoid clash with e-commerce Product model
  bookings: true
  payments: false
  outreach: true
  calendar: true

  # Commerce
  merchandise: true
  orders: true

  # CRM
  crm: true

  # Not used by Fat Big Quiz
  analytics: false
  external_api: false
  game_engine: true
  marketplace_sync: false
  purchases: false
  pricing: false
  restream: false

# Package-specific config
email:
  brandName: "Fat Big Quiz"
  style:
    primary: "#7c3aed"
    headerBg: "#7c3aed"

calendar:
  domain: "fatbigquiz.com"
  calendarName: "Fat Big Quiz Events"

bookings:
  modelName: "booking"
  brandPrefix: "FBQ"

crm:
  customerPrefix: "FBQ"
  campaignModel: "crmCampaign"
  campaignSendModel: "crmCampaignSend"
  scoring:
    booking_completed: 20
    email_opened: 2
    email_clicked: 5
    website_visit: 1
    product_used: 10
    app_interaction: 5
    returning_visitor: 5
    decay_3_months: -5
    decay_6_months: -10
    decay_12_months: -15
  marketingPreferences:
    - "Printable Quizzes"
    - "Live Shows"
    - "Free Content"
    - "Quiz Builder"

outreach:
  adminEmail: "${ADMIN_EMAIL}"
  brandName: "Fat Big Quiz"

# CORS
cors:
  origin: true
  credentials: true
  methods: ["GET", "POST", "PUT", "DELETE"]
  allowedHeaders:
    - "Content-Type"
    - "Authorization"
    - "x-admin-key"
